{
  "name": "ketowell-monorepo",
  "version": "1.0.0",
  "description": "KetoWell - Transform Your Health with the Science of Ketogenic Living",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "build:web": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "start:web": "pnpm --filter web start",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "db:push": "pnpm --filter web db:push",
    "db:studio": "pnpm --filter web db:studio"
  },
  "keywords": [
    "ketogenic",
    "health",
    "wellness",
    "ai",
    "monorepo"
  ],
  "author": "KetoWell Team",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "prettier": "^3.0.0"
  }
}
